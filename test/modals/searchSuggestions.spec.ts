import { describe, test } from 'vitest';
// This functionality uses the Obsidian API which is hard to replicate in tests.
// Will be revisited later.
describe.todo('IBookHighlightsPluginSearchModal', () => {
  test('Should filter books based on query');
  test('Should display suggestions in the modal');
  test('Should handle selection of a suggestion');
  test('Should handle no results found');
});

describe.todo('OverwriteBookModal', () => {
  test('Should show a modal window for bulk import');
  test('Should show a modal window with book details');
});
